<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             x:Class="RGS.App.Pages.HomePage"
             Title="RGS Sync">
  <ScrollView>
    <VerticalStackLayout Padding="16" Spacing="12">
      <Label Text="Unofficial Renpho → Garmin sync"
             FontAttributes="Bold" />
      <Button Text="Connect Renpho" Command="{Binding ConnectRenphoCmd}" />
      <Button Text="Connect Garmin" Command="{Binding ConnectGarminCmd}" />
      <Button Text="Sync Now (Foreground)" Command="{Binding SyncNowCmd}" />
      <Label Text="{Binding LastResult}" />
      <Button Text="Settings" Command="{Binding GoSettingsCmd}" />
      <Button Text="Logs" Command="{Binding GoLogsCmd}" />
    </VerticalStackLayout>
  </ScrollView>
</ContentPage>

